# v1.7 実装マトリクス

更新日：2026-08-02

状態の意味：

- **実装済み**：採用したv1.7範囲のコードと導線がある
- **自動QA合格**：2026-08-02のChromium自動実ブラウザQAで確認済み
- **部分実装**：基本処理はあるが、旧仕様の全オプションまたは端末横断保証はない
- **端末依存**：ブラウザが公開するWeb APIとOS共有機能に依存する
- **実機未受入**：iPhone／Android実機での結果が未記録
- **保留**：旧長期仕様として残す。v1.7採用範囲の完成条件には含めない
- **対象外**：今回のWeb/PWA版では保証しない

## 1. カメラ・撮影・画面

| 領域 | 状態 | 実装内容・制限 |
|---|---|---|
| 初期画面 | 実装済み・自動QA合格 | 起動直後カメラ。写真選択はアルバム側の補助操作 |
| カメラ画面 | 実装済み・自動QA合格 | 常設下部ナビを撤去し、全画面プレビュー中心へ変更 |
| 配色・余白 | 実装済み・自動QA合格 | 濃紺、スチールブルー、白／グレー中心。緑は成功状態等に限定 |
| 文字・タップ領域 | 実装済み・自動QA合格 | 表示文字12px以上、主要操作44px以上を自動確認。実視認性は実機未受入 |
| 写真／動画 | 実装済み・実機未受入 | 写真撮影、基本動画録画、確認、OS共有。動画編集は対象外 |
| 動画無音フォールバック | 実装済み・実機未受入 | マイク取得失敗時は映像のみ再取得し、確認画面へ無音を表示 |
| 前後カメラ | 端末依存・実機未受入 | `facingMode` で切替 |
| ズーム・ライト | 端末依存・実機未受入 | `MediaStreamTrack.applyConstraints` 対応時だけ利用 |
| 露出 | 部分実装・端末依存・実機未受入 | 実露出制約を試行し、非対応時はプレビューと撮影画像へ端末内明るさ補正 |
| タップフォーカス | 部分実装・端末依存・実機未受入 | `pointsOfInterest` とsingle-shot制約を対応時に試行 |
| AE/AF固定 | 部分実装・端末依存・実機未受入 | 長押し時にmanual制約を試行。iPhoneでの保証なし |
| 撮影補助 | 実装済み・自動QA合格・実機未受入 | 三分割、中央線、正面ガイド、水平器、3秒／10秒タイマー |
| 撮影モード | 部分実装 | モード名と推奨ガイドを切替。すべての実カメラ設定を変えるものではない |
| フリッカー軽減 | 部分実装・端末依存・実機未受入 | 50Hzは25fps、60Hzは30fpsを対応端末で試行。設定表示は自動QA合格 |
| カメラ拒否代替 | 実装済み・実機未受入 | 再試行、標準カメラ、既存写真選択を表示 |
| 撮影後端末保存 | 実装済み・自動QA合格・端末依存 | 撮影後に自動共有せず、明示ボタンでOS共有へ進む |
| 撮影後サイズ | 実装済み・自動QA合格 | 元の取得画像、長辺3840／1920／1280とJPEG画質 |
| GPS取得 | 実装済み・端末依存・実機未受入 | 候補表示・現在地操作時に最新位置を再取得 |
| 近隣撮影先 | 実装済み | 保存済み撮影先の座標を端末内で距離照合 |
| 住所候補 | 実装済み・自動QA合格・オンライン依存 | 明示操作時だけ座標をOpenStreetMapへ送る。写真は送らない |
| アルバム | 実装済み | カメラ左下サムネイルから開く補助履歴。端末写真アプリの代替ではない |
| 写真台帳 | 実装済み・自動QA合格 | アルバムで複数選択した後だけ利用。グローバルナビには置かない |

## 2. 写真編集

| 機能 | 状態 | 内容・残り |
|---|---|---|
| エディタ画面 | 実装済み・自動QA合格 | モバイルの写真領域を拡大し、上部1列・下部横ツールへ整理 |
| 水平 | 実装済み・自動QA合格 | ±10°、0.1°、グリッド、自動候補、リセット。背景ぼかしなし |
| 切り抜き | 実装済み | 元、自由、1:1、4:3、3:4、16:9、9:16、反転、ドラッグ、ピンチ |
| 回転 | 部分実装 | 右90°と水平微調整。左回転専用操作はない |
| 台形 | 部分実装 | 手動4点と射影変換。四隅自動候補はない |
| 色調整 | 実装済み・自動QA合格 | 明るさ、コントラスト、彩度、色温度、影、ハイライト、シャープを独立調整 |
| 影・反射・輪郭 | 実装済み | 影、ハイライト、シャープの独立画像処理 |
| フィルター | 実装済み・自動QA合格 | 8種類と0〜100%強度 |
| 全編集初期化 | 実装済み・自動QA合格 | 元写真状態へ戻し、直前状態へUndo可能 |
| 画像サイズ | 実装済み | 元、編集サイズ、3840、1920、1280、任意長辺 |
| 高解像度で拡大 | 実装済み | 通常補間の2倍／4倍。AI超解像ではない |
| 円・四角・線・矢印 | 部分実装 | 配置、移動、拡縮、回転、色、線幅、線種。塗り・角丸・矢印先端詳細は保留 |
| 文字・文字枠 | 部分実装 | 入力、移動、拡縮、回転、色。背景帯・書式詳細は保留 |
| 日付 | 実装済み | 4形式、色、配置後の移動・拡縮・再編集 |
| ラベル | 部分実装 | ホイール、一時カスタム語、自動色、「予備品」「再使用不可」。辞書永続編集等は保留 |
| 番号 | 部分実装 | 連番、個別編集、移動・拡縮。欠番詰め、説明一覧、形状選択は保留 |
| 寸法 | 部分実装・自動QA合格 | 矢印先行、中央移動、両端調整、タップ後mm入力・再編集。旧仕様の他単位等は保留 |
| 角度 | 実装済み | 3点指定による写真上の見かけ角度 |
| 手書き | 部分実装 | ペン、マーカー。線幅選択、注記専用消しゴム、平滑化詳細は保留 |
| 部分拡大 | 部分実装 | 移動・サイズ変更できる丸窓、約2倍。四角、3倍、4倍は保留 |
| モザイク・ぼかし・塗り | 部分実装 | 四角範囲。強度、ブラシ、任意色の詳細は保留 |
| 手動透明化 | 部分実装 | 3ブラシと同系色背景抜き。復元ブラシ、境界ぼかし、自由選択は保留 |
| コピースタンプ | 保留 | 未実装 |
| 原画比較 | 実装済み | 押している間だけ原画表示。詳細スライド比較は保留 |
| 2枚比較 | 部分実装 | 左右／上下とラベル。境界移動、重ね、各写真位置・ズームは保留 |
| Undo／Redo | 実装済み | 最大40履歴。主要操作と全編集初期化を復帰可能 |

## 3. 保存・データ・プライバシー

| 項目 | 状態 | 内容・制限 |
|---|---|---|
| JPEG／PNG／WEBP | 実装済み・端末依存 | Canvas対応形式を出力。JPEGは透明部分を白合成 |
| 画質・サイズ・ファイル名 | 実装済み | 保存画面で選択 |
| 保存設定保持 | 実装済み・自動QA合格 | 形式、画質、サイズ、任意長辺をlocalStorageへ保持 |
| 最終画素数 | 実装済み | 保存予定の幅・高さを表示 |
| 推定容量 | 実装済み・自動QA合格 | 形式・画質・画素数から概算範囲を表示。実容量保証ではない |
| 元画像保護 | 実装済み | 新しい画像として生成 |
| 写真アプリ保存 | 端末依存・実機未受入 | OS共有画面まで。実際の保存操作と結果はOS側 |
| WORK PHOTO内複製 | 実装済み | 編集保存時は初期OFF。利用者がチェックした場合だけ複製 |
| 端末内アルバム | 実装済み | IndexedDBへ保持。写真アプリとは別 |
| AI不使用 | 実装済み | AI解析、生成、OCR、AI超解像なし |
| 写真アップロード | 実装済み | 写真をアプリ独自サーバーや画像APIへ送らない |
| 住所候補通信 | 明示例外 | 利用者操作時に座標だけをOpenStreetMapへ送信 |
| オフライン編集 | 実装済み・自動QA合格 | ルート画面・エディタを再読込。住所候補はオフライン対象外 |
| EXIF／GPS埋め込み | 対象外 | Web版では保証しない |

## 4. 台帳

| 機能 | 状態 |
|---|---|
| A4縦・横、9／12／16／20枚 | 実装済み・実機未受入 |
| 通し／ページごと／手入力番号 | 実装済み・実機未受入 |
| 写真番号／No.／数字／3桁／丸数字 | 実装済み・実機未受入 |
| 下部／左上／右上 | 実装済み・実機未受入 |
| 名前付き書式 | 実装済み・実機未受入 |
| 写真別フレーミング | 実装済み・実機未受入 |
| Excel編集可能セル | 実装済み・実機未受入 |

## 5. QA・受入

| 項目 | 状態 |
|---|---|
| Chromium自動実ブラウザQA | 47／47合格、失敗0 |
| Console error／Page error／Request failure | すべて0 |
| 390×844、375×667、1280×800 | 合格 |
| ルート・エディタのオフライン再読込 | 合格 |
| iPhone Safari／ホーム画面PWA | 未実施 |
| Android Chrome／PWA | 未実施 |
| 写真・動画の端末写真アプリ保存 | 実機未受入 |
| カメラAPIの端末別対応表 | 未作成 |

Web自動QAの証跡は `QA_RESULT.md`、実機受入は `DEVICE_ACCEPTANCE_CHECKLIST.md` を正本とします。
